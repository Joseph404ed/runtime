name: CI

on:
  push:
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  test-linux:
    name: Test on Linux
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        path: runtime
    
    - name: Checkout agent-core
      uses: actions/checkout@v3
      with:
        repository: RustyAI/agent-core
        path: agent-core
    
    - name: Checkout messaging
      uses: actions/checkout@v3
      with:
        repository: RustyAI/messaging
        path: messaging
    
    - name: Checkout cognition
      uses: actions/checkout@v3
      with:
        repository: RustyAI/cognition
        path: cognition
    
    - name: Install Rust toolchain
      uses: dtolnay/rust-toolchain@stable
      with:
        components: rustfmt, clippy
    
    - name: Cache dependencies
      uses: Swatinem/rust-cache@v2
      with:
        workspaces: runtime
    
    - name: Check formatting
      working-directory: runtime
      run: cargo fmt --all -- --check
    
    - name: Build
      working-directory: runtime
      run: cargo build --verbose
    
    - name: Run Clippy
      working-directory: runtime
      run: cargo clippy --all-targets --all-features -- -D warnings
    
    - name: Run tests
      working-directory: runtime
      run: cargo test --verbose
    
    - name: Run examples
      working-directory: runtime
      run: |
        cargo run --example basic_runtime
        cargo run --example scheduled_agents
        cargo run --example isolated_agents
        cargo run --example supervised_agents

  test-windows:
    name: Test on Windows
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3
      with:
        path: runtime
    - uses: actions/checkout@v3
      with:
        repository: RustyAI/agent-core
        path: agent-core
    - uses: actions/checkout@v3
      with:
        repository: RustyAI/messaging
        path: messaging
    - uses: actions/checkout@v3
      with:
        repository: RustyAI/cognition
        path: cognition
    - uses: dtolnay/rust-toolchain@stable
    - uses: Swatinem/rust-cache@v2
      with:
        workspaces: runtime
    - working-directory: runtime
      run: cargo test --verbose

  test-macos:
    name: Test on macOS
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v3
      with:
        path: runtime
    - uses: actions/checkout@v3
      with:
        repository: RustyAI/agent-core
        path: agent-core
    - uses: actions/checkout@v3
      with:
        repository: RustyAI/messaging
        path: messaging
    - uses: actions/checkout@v3
      with:
        repository: RustyAI/cognition
        path: cognition
    - uses: dtolnay/rust-toolchain@stable
    - uses: Swatinem/rust-cache@v2
      with:
        workspaces: runtime
    - working-directory: runtime
      run: cargo test --verbose
